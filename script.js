let divs = []; // Array to hold the divs

for (let i = 0; i < 16; i++) {
    let div = document.createElement("div"); // Create a div element
    divs.push(div); // Add it to the array
}

console.log(divs); // This will log an array of 16 div elements
